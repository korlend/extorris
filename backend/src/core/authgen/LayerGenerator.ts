export default class LayerGenerator {
  private hubHeight: number;
  private hubWidth: number;
  // private nestsTypes:

  constructor(hubHeight: number, hubWidth: number) {
    this.hubHeight = hubHeight;
    this.hubWidth = hubWidth;
  }

  // generatePOI() {
    
  // }

  // generateHub() {
    
  // }

  // generateLayer() {}
}
